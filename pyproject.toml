[project]
name = "creditbridge-agent-runtime"
version = "0.2.0"
description = "Evidence-grounded transfer-credit operations agent"
requires-python = ">=3.11,<3.14"
dependencies = [
    "bedrock-agentcore",
    "strands-agents>=1.0,<2.0",
]

[tool.uv]
package = false
